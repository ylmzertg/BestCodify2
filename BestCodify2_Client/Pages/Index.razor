@page "/"

@using BestCodify2_Client.ViewModels
@using BestCodify.Models
@using BestCodify.Business.Repository.IRepository

@inject ICourseRepository _courseRepository
@inject ILocalStorageService _localStorage

<br />
<div class="col-3" style="padding:4px;">
    <div class="card mb-3">
        <div class="card-body">
            <h5 class="card-title">Course Name</h5>
            <h6 class="card-subtitle text-muted">course subtitle</h6>
        </div>
        <img class="d-block w-100" src="images/ertugrulYilmaz.jpeg" style=" width: 100% ;height:100% " />
        <div class="card-body">
            <p class="card-text">Description : .....</p>
        </div>
        <hr />
        <div class="card-body">
            <p class="card-text">Price:$9.99</p>
            <hr />
            <p class="card-text">Vote:4.5</p>
            <hr />
            <p class="card-text">Student Count: 12</p>
        </div>
        <div class="card-body">
            <button class="btn btn-warning">Detail</button>
            <button class="btn btn-primary">Add Basket</button>
        </div>
        <div class="card-footer text-muted">
            Create Date:  2 days ago
        </div>
    </div>
</div>
@code{
    //public IEnumerable<CourseViewModel> CourseVideModel { get; set; } = new List<CourseViewModel>();
    private IEnumerable<CourseDto> CourseModelDto { get; set; } = new List<CourseDto>();

    private string ErrorMessage { get; set; }


    protected override async Task OnInitializedAsync()
    {
        //var data = await _courseRepository.GetAllCourse();

        //if (!data.IsSuccess)
        //    ErrorMessage = data.Message;
        //else
        //    CourseModelDto = data.Data;
    }
}